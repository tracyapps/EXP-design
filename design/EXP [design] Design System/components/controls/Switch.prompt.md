macOS toggle switch. `mini` (default) matches the inspector's Auto Layout / Auto Padding switches.

```jsx
<Switch checked={on} onChange={setOn} />
<Switch checked={on} onChange={setOn} size="small" />
```
