Phosphor-Icons wrapper standing in for the app's native SF Symbols (closest CDN match — flag to user if exact SF parity is needed). Host page must load the Phosphor web font.

```jsx
<Icon name="cursor" />
<Icon name="lock-simple" weight="fill" size={14} color="var(--accent)" />
```
